{
  "name": "@witty-services/ngx-common",
  "version": "1.0.0-rc.2",
  "description": "Type repository core library",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "repository": {
    "type": "git",
    "url": "http://github.com/witty-services/ngx-common"
  },
  "scripts": {
    "clean": "rm -R -f dist",
    "build": "npm run clean && npm run lint && tsc",
    "build:watch": "tsc -w",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --coverage",
    "test:ci": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "publish:lib": "npm run build && npm publish --access=public"
  },
  "author": "Romain MARTINEAU",
  "license": "MIT",
  "peerDependencies": {
    "reflect-metadata": "^0.1.13",
    "@angular/core": ">= 6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.17",
    "@angular/core": "^9.0.0",
    "reflect-metadata": "^0.1.13",
    "jest": "^24.9.0",
    "rxjs": "^6.5.4",
    "ts-jest": "^24.2.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
